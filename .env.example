# FactoryForge does not require any environment variables for its core
# gameplay, simulation, rendering, or local save/load features.
#
# This file is a placeholder for future optional integrations
# (e.g. a hosted optimization-assistant API). Copy to .env and fill in
# values only if such a feature is added and you choose to enable it.
